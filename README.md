# docker-model-sentiment

Perform basic sentiment analysis using the new Docker Model Runner.

To build and run:
```
docker compose up
```

Note the first service will pull the appropriate model to the local machine. The second service runs the application to determine sentiment.